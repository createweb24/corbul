/** Publicitate (ADS-SPEC §3) — spațiile de banner și ajutoarele lor. */

export { AdSlot, type AdSlotProps } from "./AdSlot";
export {
  AD_ZONE_SIZES,
  DEFAULT_AD_SIZE,
  adZoneSize,
  type AdSize,
} from "./zones";
export { splitAfterParagraph, type SplitContent } from "./splitContent";
